interface:
  display_name: "Citadel"
  short_description: "OSS text guard + optional paid multimodal scans"
  default_prompt: "Use $citadel to secure an LLM app: default to the free OSS CLI/sidecar for text-only input/output scans, and use the paid Citadel Gateway /v1/scan for multimodal files (images/PDFs/docs) or when an API key is provided."

